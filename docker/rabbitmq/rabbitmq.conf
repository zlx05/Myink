# Myink RabbitMQ 配置（compose 挂载到 /etc/rabbitmq/rabbitmq.conf）。
#
# consumer_timeout：批次任务单章生成可能超过 RabbitMQ 默认 30 分钟看门狗（超时未
# 消费/发送心跳的消息会被判失联而 requeue → 重复消费）→ 放宽到 6 小时，与 pika
# 客户端 heartbeat=1800 配套，保证 LangGraph 长批次通道不被服务端杀掉。
consumer_timeout = 21600000
