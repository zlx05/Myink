# 项目演示与验证说明

## 项目定位

面向长篇网文的创作工作台，重点是多阶段生成的状态管理、可审计的一致性检查，以及失败后恢复。当前是本地 MVP；可展示完整业务流程，不将测试替身的表现等同于真实模型效果。

## 推荐演示顺序（约 10 分钟）

1. **建书与约束**：展示已有作品的世界观、大纲、人物卡片，说明哪些设定由作者确认。
2. **生成编排**：预先准备一次批次任务，展示规划、写作、抽取、校验、审核、落库的节点记录，以及耗时和模型 usage。不要现场依赖外部模型一定及时响应。
3. **人工确认和续跑**：展示等待审核任务与候选池，解释 checkpoint 如何保留进度。实际演示前用自己的测试作品走通一次。
4. **编辑可靠性**：改正文后切章再切回，展示草稿恢复；打开同一章的两个标签页，先在 A 保存，再在 B 保存，展示 409 冲突以及服务器正文对比。
5. **记忆与校验**：展示事实、人物状态、关系图与伏笔；说明低风险候选自动处理，高风险候选需要人工确认。
6. **验证证据**：运行前端测试或展示本次回归记录，打开并发保存和上下文预算测试，解释它们防止的具体故障。

## 可以用代码与测试支撑的技术点

| 技术点 | 证据入口 | 说明边界 |
|---|---|---|
| 批次与单章编排 | `workflow/batch_graph.py`、`chapter_graph.py`、`tests/test_flow.py` | 流程回归使用模型替身 |
| 并发旧版本保护 | `api/routes_chapters.py`、`tests/test_chapter_versions.py` | 版本号检查在行锁内；冲突不会写历史或正文 |
| 草稿恢复和异步保存 | `web/src/components/ChapterEditor.test.tsx` | 草稿按账号/章节隔离，存于当前标签页，关闭前需服务器保存 |
| 提示词预算 | `context_budget.py`、`tests/test_context_budget.py` | 完整消息估算，保留硬约束；不是精确 tokenizer |
| 队列与恢复 | `worker/`、`gateway/internal/queue/`、`tests/test_multiprocess.py` | RabbitMQ 管任务，Redis 管锁/限流/SSE；不宣称 exactly-once |
| 租户隔离 | `db.py`、`api/auth.py`、`tests/test_auth.py` | RLS 与归属校验已实现；演示用户名登录不等于正式身份认证 |
| 契约一致性 | `spec/api-openapi.json`、Python/Go 契约测试 | 请求/响应修改必须同步契约与消费者 |

## 不能混用的指标

40 条冲突样例中的 21/26 检出、0/14 误报是机制测试基线。LLM 判定和 embedding 使用替身，且部分阴性测试没有对应检测机制。它不支持“真实小说矛盾检出率 80.8%”或“误报为零”的结论。

模型调用 usage/耗时是真实接入后的运行记录；单元测试中的 token 数与耗时可能是固定的替身数据。展示时要标明来源。未做压力测试前，不宣称具体 QPS、并发用户数或高可用水平。

## 下一阶段：真实模型效果评测

以下是尚未执行的验证方案，不是已有结果：

- 固定模型版本、提示词版本、采样参数与题材；每个条件重复多次，记录失败与人工介入。
- 先用短篇小样本校准标注标准，再在不同题材连续生成 30–50 章；保存初稿与人工修改稿。
- 对照条件至少包含完整系统、关闭混合召回、关闭复盘；每次只改变一个模块。
- 指标包括每万字矛盾数、桥段重复、伏笔兑现、人工修改量、每章成本、耗时及失败率。
- 由人工盲评标注证据，模型评审只作辅助；报告样本量、模型配置、平均值和波动。
- 原始稿件、标注、配置、运行记录一并保存，形成可重现报告。

## 后续优先级

1. 真实模型与长篇数据上的效果评测，先证明核心模块带来的改善。
2. 修复已知漏检：经历矛盾、物品状态、伏笔兑现、能力前置规则、势力归属。
3. 作者效率：选段改写、差异预览、正文与记忆同步状态、已有稿件导入。
4. 计划公网试用时，再做正式账户、迁移、备份恢复和运维监控。

## 2026-09-08 本地验证记录

- 独立临时 PostgreSQL / Redis / RabbitMQ，未连接开发作品库，模型与 embedding 使用替身。
- Python 全量：509 passed、5 xfailed；5 个 xfail 是既有冲突检测缺口。
- 随后补充手工记忆校正超预算场景，并运行预算 / Provider / 契约聚焦回归：58 passed。
- 前端：69 passed（包含 8 项编辑器交互测试），TypeScript / Vite 生产构建通过。
- Go：vet 与全部测试通过，无 SKIP；包括真实 Redis / RabbitMQ 集成测试。
- Compose 文件校验、YAML 解析与 Bash 脚本语法检查通过。
- 尚未执行真实模型长篇评测、整套 Docker 镜像重建或远端 GitHub Actions。现有前端仍有 6 项 lint 警告和主包体积提示；演示默认 JWT 密钥在 Python 测试中触发长度警告。

这份记录是当前工作区的本地验证结果，后续代码变更后应重新运行相关检查。
