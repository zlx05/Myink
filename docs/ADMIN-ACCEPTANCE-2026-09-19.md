# 管理员与写作观测验收记录

当前状态：功能审查、隔离验收、正式账号切换和部署后HTTP验收完成。此记录不代表已完成公网安全加固。密码不保存于本文件。

## 已验证

| 检查 | 结果 |
| --- | --- |
| Python 全量（最终后端修正版） | 784 passed，5 xfailed，180.40秒，无警告 |
| Python 语法检查 | compileall通过 |
| Go静态检查与全量测试 | vet通过；test通过，未出现SKIP |
| 管理员真实HTTP验收 | 59项通过 |
| 原登录/写作隔离真实HTTP验收 | 39项通过 |
| 后端独立审查 | 完成；修复业务字段被截断、URL脱敏性能与相邻URL漏脱敏问题，复审通过 |
| 前端全量（最终修正版） | 38文件、246项通过；lint退出0，5条既有警告；生产构建通过，保留既有大包提示 |
| 实际页面 | 隔离18080验证六个管理视图、用户筛选、跨用户章节全文、上下文、任务节点/召回脱敏、无任务编号节点及访问审计；默认及768像素窄屏布局检查，浏览器无error日志 |
| 最终整体验收审查 | 通过，无Critical/Important；保留一项费用标题文案建议 |
| 正式账号应用与数据保留 | zlx/admin登录成功，原UUID保留，7本作品/44章；其他项目及用户27表摘要一致 |
| 正式服务 | 六个服务运行，API/PG/Redis/RabbitMQ健康，网关readyz为ok；管理任务/节点/审计接口通过 |
| 正式浏览器验收 | zlx网页登录成功；作品库4本正式作品+3本待完成草稿；管理总览显示2用户/7作品/44章/372任务/653节点；验收页保留在/admin |

Python使用`python -m pytest tests/ -q -p no:cacheprovider`。五项xfail来自已有冲突样例：首次来访、已毁物品再现、伏笔兑现、能力规则、势力归属机制尚未全部实现；不计为通过，也不等同真实小说质量评测。旧的一次运行曾出现pytest缓存权限警告，最终使用禁用缓存的配置后无该警告。

Go使用`go vet ./...`和`go test -count=1 -v ./...`。本次同步了登录测试夹具缺失的role字段，独立复审通过，未改变实际认证行为。

前端任务审查修复了详情刷新仍显示旧任务状态的问题（回归测试先失败再通过），并将节点“正常”改为“未降级”，避免把降级标记误读为执行成功。独立复审通过。静态检查的5条既有警告未伪报为零警告。

## HTTP验收范围

隔离API/网关绑定18100/18080，仅连接PostgreSQL15432、Redis16380和RabbitMQ15673。没有启动测试消费者或真实模型调用；就绪检查中的worker降级是这一隔离配置的预期结果，并非已验收真实模型写作。测试夹具保留在专用测试服务中，没有删除正式数据。

- 8位字母数字密码注册、登录；无邀请码不能注册；注册提交admin角色不能提权。
- 管理员读取其他用户的章节、任务、节点与上下文；普通用户、伪造身份头、撤销角色/会话被拒绝。
- 普通写作接口仍按作品归属校验，管理员也不能借此修改他人的作品。
- 分页上限、批次节点关联、单独建书节点、token/估算费用/耗时聚合、正文读取、脱敏与no-store。
- 原有SSE、写作入口、改密撤销旧会话、退出撤销会话和另一账号不受影响。
- 超长记录不会改变原有规划版本或审核依据；新增快照有界。网址脱敏补测覆盖长文本与相邻序列化网址。

## 账号操作边界

用户明确要求不新建备份：2026-09-19正式执行，删除原zlx账号（20bbfd20-250c-4106-8be0-0c9458ccff08）及其1本作品、1章、1个任务、7条节点记录；删除其41条checkpoint_writes、12条checkpoint_blobs、12条checkpoints和1个精确关联Redis键。此删除没有应用内恢复入口，本次未新建备份。

原demo账号原地改名zlx并授予admin，UUID仍为1c54055f-b419-4dd5-a523-54b37aa8ec16，7本作品与44章保留；vipdemo不变。事务内27表受保护数据摘要一致。操作前全部任务终态、三个队列ready/unacked均0，网关/worker先暂停。新API无种子初始化，操作后网关/worker恢复；旧demo登录拒绝，数据库仅zlx/vipdemo两个账号。正式总量7项目、44章、372任务、653节点记录。所有密码经隐藏输入提供，未写入本文件。

部署复用了既有myink_pgdata_v2卷；Compose提示该卷不是它创建的，本次未清理或重建该卷。测试HTTP容器在验收后仅停止，不删除数据或卷。

## 仍需注意

管理接口只读，不授予服务器命令执行能力。费用为已记录估算，任务时间跨度包含等待，未测量或历史未记录的内容不能补造。新增快照和管理响应有限额，原流程依赖的业务字段保留完整存储。脱敏不是任意秘密识别器。

非阻断文案问题：总览目前将cost_est标为“预估存储成本”并显示“¥/US$”；实际是已有模型计价契约中的调用估算费用（元），不是存储费用或多币种账单。后续应统一标题和币种。前端过滤/详情切换竞态及一般错误重试还可增加专项测试，现有账户切换/403卸载已有回归。

现阶段管理查询使用已有管理数据库连接内的只读事务，并非专用最小权限报表账号。上线前仍须完成[公网检查清单](PUBLIC-DEPLOYMENT-CHECKLIST.md)，不能将本次功能验收解释为已经适合直接开放公网。使用方法见[管理面板说明](ADMIN.md)。
